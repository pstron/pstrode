```cpp
#include <bits/stdc++.h>
using i64 = std::int64_t;
void solve() {
}
int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    int t = 1;
    std::cin >> t;
    for (int i = 0; i < t; ++i) {
        solve();
    }
    return 0;
}
```

本项目的 C++ 模板代码，若无特殊说明，默认使用 **C++17 或更高版本** 的标准，使用 0 索引下标，左开右闭表示区间。
