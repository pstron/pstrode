[project]
name = "pstrode"
version = "0.2.0"
description = "pstron's XCPC algorithm templates and notes"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mkdocs-material>=9.7.6",
    "pymdown-extensions>=10.21.3",
]
